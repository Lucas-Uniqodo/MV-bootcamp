const array = ['apples','pears','bananas'];

const arrayLength = array.length;
console.log('array length is ' + arrayLength);

const firstElement = array[0]; // arrays start at index 0
console.log('first element is ' + firstElement);

// TODO - now try to print the second and third elements

console.log('second element is ' + array[1]);
console.log('second element is ' + array[2]);
